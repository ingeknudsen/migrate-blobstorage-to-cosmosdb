name: radix-web-console-pr
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Bootstrap
        shell: powershell
        run: |
          .\scripts\bootstrap.ps1
